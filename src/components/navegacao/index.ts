/**
 * Componentes de navegação (chrome de app). Não conhecem domínio — recebem
 * itens/grupos via props e disparam callbacks. Quem é o "Cliente > Listar"
 * é decisão da feature, não do componente de UI.
 */

export * from "./BarraInferior";
export * from "./Sidebar";
